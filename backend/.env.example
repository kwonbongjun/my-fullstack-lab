PORT=3001
NODE_ENV=development

# Local development (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/mydb"

# =============================================================
# Cloud Run + Cloud SQL 설정
# =============================================================
# Cloud SQL Unix Socket 연결 (Cloud Run에서 사용)
# 형식: postgresql://USER:PASSWORD@localhost/DATABASE?host=/cloudsql/CONNECTION_NAME
# 예시: postgresql://postgres:mypassword@localhost/mydb?host=/cloudsql/my-life-diary-99fa1:asia-northeast3:mydb-instance
#
# GCP Secret Manager에 저장할 값:
# DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@localhost/DB_NAME?host=/cloudsql/my-life-diary-99fa1:asia-northeast3:YOUR_INSTANCE_NAME"
